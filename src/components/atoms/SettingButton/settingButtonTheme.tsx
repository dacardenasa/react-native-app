import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonBox: {
    flexDirection: 'column',
    alignItems: 'center',
  },
  avatar: {
    width: 150,
    height: 150,
    borderRadius: 100,
  },
  buttonTitle: {
    fontSize: 16,
    color: '#000',
  },
});
